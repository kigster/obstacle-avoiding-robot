# Vehicle that Avoids Obstacles

Based on the BackSeatDriver library.

## Building

To build this project, run:

```bash
# run this only once
bin/setup

# and then build
bin/build

# and to clean
bin/build clean
```
